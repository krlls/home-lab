http:
  routers:
    ksmi:
      entryPoints:
        - "websecure"
      rule: "Host(`ksmi.me`)"
      service: ksmi_site
      tls:
        certresolver: mydnschallenge

  services:
    ksmi_site:
      loadBalancer:
        servers:
          - url: "http://ksmi_site:3000"

