http:
  routers:
    ksmi:
      entryPoints:
        - "websecure"
      rule: "Host(`ksmi.me`)"
      tls:
        certresolver: mydnschallenge
      service: ksmi_site

  services:
    ksmi_site:
      loadBalancer:
        servers:
          - url: "http://172.18.0.9:3000"

