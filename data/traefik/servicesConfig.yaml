http:
  routers:
    ksmi-site:
      entryPoints:
        - "websecure"
      rule: "Host(`ksmi.me`)"
      service: "ksmi-site"
      tls:
        certresolver:
          - "mydnschallenge"

  services:
    ksmi-site:
      loadBalancer:
        servers:
          - url: "http://ksmi_site:3000"

