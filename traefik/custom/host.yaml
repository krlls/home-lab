http:
  routers:
    web:
      entryPoints:
        - "web"
      rule: "Host(`web.example.com`)"
      service: web

  services:
    web:
      loadBalancer:
        servers:
          - url: http://web:80
