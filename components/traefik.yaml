version: "3.7"

networks:
  default:
    name: traefik-network
    external: true

services:
  traefik:
    restart: unless-stopped
    image: traefik:latest
    read_only: true
    container_name: traefik
    ports:
      - "80:80"
      - "443:443"
      - "8081:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ../data/traefik/traefik.yaml:/traefik.yaml:ro
      - ../data/traefik/etc/traefik:/etc/traefik/
      - ../data/traefik/srv/letsencrypt:/letsencrypt
    labels:
      - traefik.http.middlewares.vpnlist.ipwhitelist.sourcerange=192.168.0.0/24, 10.13.13.0/24, 127.0.0.1/32, 185.123.53.148/32
